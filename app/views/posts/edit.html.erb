<h1>Editing Post</h1>
<%#= image_tag(File.join(root_url,@post.image.id.to_s, "medium","serve_image")) %>

<%= render 'form' %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
<br/>
<br/>
<%= link_to 'Delete post', @post, method: :delete, data: { confirm: 'Are you sure?' } %>
